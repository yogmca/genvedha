# NPM Configuration for Production Deployment
# Ensure devDependencies are installed for build process

# Install dev dependencies even in production mode
# (needed for webpack build)
production=false
