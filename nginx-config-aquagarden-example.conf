# Nginx Configuration for AquaGarden Paradise App
# Domain: aquagarden.com
# Backend Port: 5006
# Location: /etc/nginx/conf.d/aquagarden.conf

# HTTP Server Block (Initial - Before SSL)
server {
    listen 80;
    listen [::]:80;
    server_name aquagarden.com www.aquagarden.com;

    # Let's Encrypt SSL validation path
    location ^~ /.well-known/acme-challenge/ {
        allow all;
        root /var/www/certbot;
        default_type "text/plain";
        try_files $uri =404;
    }

    # Serve React frontend from build folder
    root /home/ec2-user/genvedha-website/genvedha-llm-service/generated-apps/aquagarden-paradise-6f21e588/frontend/build;
    index index.html index.htm;

    # Handle React Router - all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to backend on port 5006
    location /api {
        proxy_pass http://localhost:5006;
        proxy_http_version 1.1;
        
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        
        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Don't cache API responses
        proxy_cache_bypass $http_upgrade;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Static files caching (images, CSS, JS, fonts)
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Logging
    access_log /var/log/nginx/aquagarden_access.log;
    error_log /var/log/nginx/aquagarden_error.log;
}

# HTTPS Server Block (Added automatically by Certbot)
# After running: sudo certbot --nginx -d aquagarden.com -d www.aquagarden.com
# Certbot will add the HTTPS configuration below:

# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     http2 on;
#     server_name aquagarden.com www.aquagarden.com;
#
#     # SSL certificates (managed by Certbot)
#     ssl_certificate /etc/letsencrypt/live/aquagarden.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/aquagarden.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # Same configuration as HTTP block above
#     root /home/ec2-user/genvedha-website/genvedha-llm-service/generated-apps/aquagarden-paradise-6f21e588/frontend/build;
#     index index.html index.htm;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api {
#         proxy_pass http://localhost:5006;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
#
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot|webp)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#
#     access_log /var/log/nginx/aquagarden_access.log;
#     error_log /var/log/nginx/aquagarden_error.log;
# }
